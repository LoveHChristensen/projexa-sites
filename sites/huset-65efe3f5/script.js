const filterButton=document.getElementById('filterButton');const filterMenu=document.getElementById('filterMenu');const cards=[...document.querySelectorAll('.task-card')];filterButton.addEventListener('click',()=>{filterMenu.hidden=!filterMenu.hidden});document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{const value=button.dataset.filter;document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('active'));button.classList.add('active');cards.forEach(card=>card.style.display=value==='all'||card.dataset.status===value?'flex':'none');filterButton.firstChild.textContent='Visa: '+button.textContent+' ';filterMenu.hidden=true}));document.querySelectorAll('.complete-button').forEach(button=>button.addEventListener('click',()=>{const card=button.closest('.task-card');const isDone=card.dataset.status==='done';card.dataset.status=isDone?'todo':'done';card.classList.toggle('complete',!isDone);const status=card.querySelector('.status');status.className='status '+(isDone?'todo':'done');status.textContent=isDone?'Att göra':'✓ Klart';button.textContent=isDone?'✓':'○';button.setAttribute('aria-label',isDone?'Markera ej klar':'Markera klart')}));